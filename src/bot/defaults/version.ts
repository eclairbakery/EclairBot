export const eclairBotVersion: {
    changes: string[],
    codename: string,
    branch: string,
    version: `${number}.${number}.${number}`
} = {
    changes: [
        'rewrited EclairBOT from ground up'
    ],
    codename: 'Strawberry',
    branch: 'eclairbot-rewrite-final-boss',
    version: '1.0.0'
};